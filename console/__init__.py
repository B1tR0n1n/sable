"""OVERLORD × SABLE console — the closed remediation loop.

    DETECT (SABLE) → PROPOSE (planner) → GATE (policy) → EXECUTE (OVERLORD) → VERIFY (SABLE) → RECEIPT

Three roles, never blurred: SABLE's models are the source of truth, the LLM
is an analyst that only proposes, and OVERLORD is the only component that
changes anything. Everything speaks three contracts: Finding, Plan, Receipt.
"""
